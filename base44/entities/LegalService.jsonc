{
  "name": "LegalService",
  "type": "object",
  "properties": {
    "profile_id": {
      "type": "string"
    },
    "practice_area_id": {
      "type": "string",
      "description": "Related practice area ID"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string",
      "maxLength": 500
    },
    "legal_category": {
      "type": "string",
      "enum": [
        "Immigration",
        "Civil",
        "Criminal"
      ],
      "description": "Legal category"
    },
    "is_active": {
      "type": "boolean",
      "default": true
    },
    "order": {
      "type": "number",
      "default": 0
    }
  },
  "required": [
    "profile_id",
    "name"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": null,
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}