{
  "name": "NFCDevice",
  "type": "object",
  "properties": {
    "profile_id": {
      "type": "string",
      "description": "Assigned profile ID (personal or business profile). Optional until activated."
    },
    "device_type": {
      "type": "string",
      "enum": [
        "card",
        "metal_card",
        "keychain",
        "bracelet",
        "stand",
        "badge",
        "sticker",
        "tag"
      ],
      "default": "card",
      "description": "Physical product type \u2014 persists after assignment so a keychain fob stays a keychain fob regardless of what it's assigned to"
    },
    "device_code": {
      "type": "string",
      "description": "Unique identifier printed on device e.g. BG-000001"
    },
    "product_sku": {
      "type": "string",
      "description": "Shop catalog SKU for this physical product (e.g. 'nfc_key_fob', 'nfc_metal_card')"
    },
    "product_name": {
      "type": "string",
      "description": "Human-readable product name e.g. 'NFC Key Fob', 'NFC Pet Collar Tag', 'NFC Luggage Tag' \u2014 persists after assignment"
    },
    "product_image": {
      "type": "string",
      "description": "Product image URL from the shop catalog \u2014 persists after assignment for consistent display"
    },
    "account_id": {
      "type": "string",
      "description": "Owner user ID who activated/claimed this device. Used to detect 'already assigned to another account'."
    },
    "assigned_asset_id": {
      "type": "string",
      "description": "Direct reference to AssetItem if assigned to an asset (enables efficient recovery routing without reverse lookup)"
    },
    "assigned_business_id": {
      "type": "string",
      "description": "Set to the profile_id when the assigned profile is a business-type plan (business, corporate, salon, restaurant, lawfirm). Enables business vs personal routing distinction."
    },
    "status": {
      "type": "string",
      "enum": [
        "available",
        "assigned",
        "active",
        "lost",
        "disabled",
        "replaced"
      ],
      "default": "available"
    },
    "assigned_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the device was first activated"
    },
    "last_assigned_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the device was last (re)assigned to its current target"
    },
    "lost_show_phone": {
      "type": "boolean",
      "default": true,
      "description": "Show owner phone number on lost item page"
    },
    "replaced_by_code": {
      "type": "string",
      "description": "Device code of the replacement device"
    },
    "description": {
      "type": "string",
      "description": "Device description or notes",
      "maxLength": 1000
    }
  },
  "required": [
    "device_type",
    "device_code"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.profile_id": {
            "$in": [
              "{{user.data.owned_profile_ids}}"
            ]
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.profile_id": {
            "$in": [
              "{{user.data.owned_profile_ids}}"
            ]
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.profile_id": {
            "$in": [
              "{{user.data.owned_profile_ids}}"
            ]
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}