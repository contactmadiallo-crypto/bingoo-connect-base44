/**
 * useFirestoreNotifications — stub until Firebase npm package is installed.
 * Returns empty arrays so callers work without any changes.
 */
export function useFirestoreNotifications(_userId) {
  return { notifications: [], unreadCount: 0, error: null };
}